## Assignment

1. [x] Create an account on Google AI Studio if you don't already have one.
2. [x] Click the "Create API Key" button. Here are the docs if you get lost.
3. [x] Copy the API key and paste it into a new `.env` file in your project directory. The file should look like this:

```
GEMINI_API_KEY='your_api_key_here'
```

4. [x] Add the `.env` file to your `.gitignore`.

<blockquote style="background-color: #2d0405; border-left: 5px solid #ea4045; padding: 5px 10px; margin: 10px auto">
**never commit API keys, passwords, or other sensitive information to Git.**
</blockquote>

5. [x] Update the `main.py` file. When the program starts, load the environment variables from the `.env` file using the `dotenv` library, and read the API key:

```python
import os
from dotenv import load_dotenv

load_dotenv()
api_key = os.environ.get("GEMINI_API_KEY")
```

6. [x] If the environment variable wasn't found – i.e., if `api_key` is `None` – raise a `RuntimeError` with a helpful message.
7. [x] Import the `genai` library and use the API key to create a new instance of a Gemini client:

```python
from google import genai

client = genai.Client(api_key=api_key)
```

8. [ ] Use the client.models.generate_content() method to get a response from the gemini-2.5-flash model! You'll need to use two named parameters:
    - `model`: The model name, `gemini-2.5-flash` (this one is still available on the free tier, albeit with increasingly strict limits)
    - `contents`: The prompt to send to the model (a string). For now, *hard-code this prompt exactly*:
        ```
        "Why is Boot.dev such a great place to learn backend development? Use one paragraph maximum."
        ```

9. [ ] The `generate_content` method returns a `GenerateContentResponse` object. Print the `.text` property of the response to see the model's answer.
    
```console
Boot.dev excels as a backend learning platform due to its highly interactive, text-based curriculum that emphasizes hands-on coding. Learners engage directly with in-browser exercises and build real-world projects, fostering a deep understanding of core backend concepts before diving into practical applications with languages like Go and Python. Its structured, gamified approach helps maintain momentum and tracks progress, making complex topics digestible and highly engaging for aspiring backend developers.
```